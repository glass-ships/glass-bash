#!/bin/bash

alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias ssh='ssh -t -o PasswordAuthentication=no -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'
alias lesss='less -RSI +G'
alias colorize="perl $PWD/colorize/colorize"

which hub >/dev/null && alias git=hub
